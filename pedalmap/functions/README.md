# DEPRECATED for PedalMap MVP
#
# Firebase Cloud Functions require Blaze (billing). PedalMap stays on Spark (0 €).
# Use Cloudflare Workers instead: ../workers/api
#
# This folder is kept only as historical reference and must NOT be deployed
# while the cost goal is €0 with no income.
