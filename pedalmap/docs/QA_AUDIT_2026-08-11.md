# QA / seguridad profunda — 2026-08-11

Branch: `cursor/pedalmap-qa-audit-deep-83e6` (sobre punta emails/alertas/trial).

## Reparado en esta pasada

| Sev | Hallazgo | Fix |
|-----|----------|-----|
| P0 | Create `users` podía nacer como `premium` | Rules: `plan == 'free'` en create |
| P0 | `usage` monthKey rollover sin techo | Rules: rollover solo a mes UTC real; creates 0–1 |
| P0 | `withinSaveLimit` petaba sin usage | Safe defaults + plan check |
| P1 | `routeShares` hijack (update sin owner) | Update solo dueño; userId inmutable |
| P1 | Webhook Stripe sin skew de `t` | Rechaza \|now−t\| > 300s |
| P1 | Anon UID churn en routing | RL extra por IP para anónimos |
| P1 | Alert email marcado sent si Resend stub | Solo `markAlertEmailSent` si `sent` |
| P2 | 500 Worker filtraba `error.message` | Respuesta genérica `internal` |
| P2 | Landing omitía trials / trial anual | Cards con `FREE_TRIALS` + `ANNUAL_TRIAL_DAYS` |
| P2 | Legal prometía borrado en perfil | Copy solo por email a soporte@ |
| P2 | Copy avisos “proactivos” | Honest: al abrir Mis rutas |
| P2 | Label Objetivo “(Premium)” | “opcional” |
| P2 | Share Mis rutas solo `makePublic` | `publishForShare` (Worker) |
| — | SW | `pedalmap-shell-v24` |

## Ya sólido (no tocado)

- Routing Worker con Bearer + create quota Admin
- Client allowlist eliminado; Premium ops vía secret
- Correos marca `@pedalmap.es` en UI
- Stripe `trialing` → premium; trial anual 7d
- GPX soft trial Free en cliente
- ORS key no en SPA

## Riesgos residuales (aceptados / backlog)

| Sev | Nota |
|-----|------|
| P1 | GPX es paywall suave (geometría ya en cliente) |
| P2 | Rankings / efforts self-write (community forgeable) |
| P2 | Contadores `publicProfiles` libres |
| P3 | Functions legacy `orsProxy` sin auth si alguien lo despliega |
| P3 | Rotar `RESEND_API_KEY` expuesta en chat |

## Deploy checklist

```bash
npx firebase deploy --only firestore:rules --project pedalmap-79b3a
npm run worker:deploy
npm run deploy:hosting
```
